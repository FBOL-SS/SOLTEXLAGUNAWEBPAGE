```liquid
<footer class="bg-gray-900 text-white py-12">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
      <div>
        <h3 class="text-xl font-bold mb-4">{{ shop.name }}</h3>
        <p class="text-gray-400 mb-4">Industrial sewing solutions for manufacturers across Mexico and Central America.</p>
        <div class="flex gap-4">
          <a href="#" class="text-gray-400 hover:text-white transition-colors">
            {% render 'icon-facebook' %}
          </a>
          <a href="#" class="text-gray-400 hover:text-white transition-colors">
            {% render 'icon-instagram' %}
          </a>
          <a href="#" class="text-gray-400 hover:text-white transition-colors">
            {% render 'icon-linkedin' %}
          </a>
        </div>
      </div>

      <div>
        <h4 class="font-semibold mb-4">Quick Links</h4>
        <ul class="space-y-2">
          {% for link in linklists.footer-1.links %}
            <li><a href="{{ link.url }}" class="text-gray-400 hover:text-white transition-colors">{{ link.title }}</a></li>
          {% endfor %}
        </ul>
      </div>

      <div>
        <h4 class="font-semibold mb-4">Products</h4>
        <ul class="space-y-2">
          {% for link in linklists.footer-2.links %}
            <li><a href="{{ link.url }}" class="text-gray-400 hover:text-white transition-colors">{{ link.title }}</a></li>
          {% endfor %}
        </ul>
      </div>

      <div>
        <h4 class="font-semibold mb-4">Contact</h4>
        <ul class="space-y-3 text-gray-400">
          <li class="flex items-start gap-2">
            {% render 'icon-map-pin' %}
            <span>Av. Industrial 1234, Monterrey, Mexico</span>
          </li>
          <li class="flex items-center gap-2">
            {% render 'icon-phone' %}
            <span>+52 81 1234 5678</span>
          </li>
          <li class="flex items-center gap-2">
            {% render 'icon-mail' %}
            <span>info@soltexlaguna.com</span>
          </li>
        </ul>
      </div>
    </div>

    <div class="border-t border-gray-800 mt-8 pt-8 flex flex-col md:flex-row justify-between items-center">
      <p class="text-gray-400">{{ settings.footer_copyright }}</p>
      <div class="flex gap-6 mt-4 md:mt-0">
        {% for link in linklists.footer.links %}
          <a href="{{ link.url }}" class="text-gray-400 hover:text-white transition-colors">{{ link.title }}</a>
        {% endfor %}
      </div>
    </div>
  </div>
</footer>

{% schema %}
{
  "name": "Footer",
  "settings": [],
  "blocks": [
    {
      "type": "link_list",
      "name": "Link list",
      "settings": [
        {
          "type": "link_list",
          "id": "menu",
          "label": "Menu"
        }
      ]
    }
  ]
}
{% endschema %}
```