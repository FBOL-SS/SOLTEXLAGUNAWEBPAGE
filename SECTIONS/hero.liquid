```liquid
<div class="relative bg-gradient-to-r from-navy to-blue text-white py-20 md:py-28">
  <div class="container mx-auto px-4">
    <div class="max-w-3xl mx-auto text-center">
      <h1 class="text-4xl md:text-5xl font-bold mb-6">{{ section.settings.heading }}</h1>
      <p class="text-xl mb-8">{{ section.settings.subheading }}</p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="{{ section.settings.cta1_link }}" class="bg-accent hover:bg-blue-700 text-white font-bold py-3 px-8 rounded-lg transition-colors">
          {{ section.settings.cta1_text }}
        </a>
        <a href="{{ section.settings.cta2_link }}" class="border-2 border-white hover:bg-white hover:text-navy text-white font-bold py-3 px-8 rounded-lg transition-colors">
          {{ section.settings.cta2_text }}
        </a>
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Hero",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Precision Sewing Solutions"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Industrial-grade machines and automation for apparel production"
    },
    {
      "type": "text",
      "id": "cta1_text",
      "label": "Primary CTA Text",
      "default": "Explore Products"
    },
    {
      "type": "url",
      "id": "cta1_link",
      "label": "Primary CTA Link"
    },
    {
      "type": "text",
      "id": "cta2_text",
      "label": "Secondary CTA Text",
      "default": "Request Quote"
    },
    {
      "type": "url",
      "id": "cta2_link",
      "label": "Secondary CTA Link"
    }
  ],
  "presets": [
    {
      "name": "Hero",
      "category": "Content"
    }
  ]
}
{% endschema %}
```