
{% layout none %}
<!doctype html><meta charset="utf-8">
<title>404 — {{ shop.name }}</title>
<link rel="stylesheet" href="{{ 'theme.css' | asset_url }}">
<section style="min-height:100dvh;display:grid;place-items:center">
  <div style="text-align:center">
    <h1 class="section-title">Página no encontrada</h1>
    <p class="section-sub">La ruta no existe o fue movida.</p>
    <a class="btn btn-primary" href="{{ routes.root_url }}">Volver al inicio</a>
  </div>
</section>
