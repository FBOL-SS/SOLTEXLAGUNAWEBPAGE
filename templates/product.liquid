{%- comment -%}
Product template limpio.
- Breadcrumb y botón "Volver" sin imágenes (evita ImgWidthAndHeight).
- Enlaces usando el objeto routes (evita HardcodedRoutes).
- Todo el contenido del producto se renderiza desde la sección `soltex-product`.
{%- endcomment -%}

{%- assign back_collection = collection | default: product.collections.first -%}

<nav aria-label="breadcrumb" style="margin:8px 0 14px; font-size:14px; padding:0 16px; max-width:1200px; margin-left:auto; margin-right:auto;">
  <a href="{{ routes.root_url }}">Inicio</a>
  {%- if back_collection -%}
    &nbsp;/&nbsp;<a href="{{ back_collection.url }}">{{ back_collection.title }}</a>
  {%- else -%}
    &nbsp;/&nbsp;<a href="{{ routes.all_products_collection_url }}">Catálogo</a>
  {%- endif -%}
  &nbsp;/&nbsp;<span aria-current="page">{{ product.title }}</span>
</nav>

<div style="max-width:1200px; margin:0 auto 8px; padding:0 16px;">
  {%- assign back_url = back_collection.url | default: routes.all_products_collection_url -%}
  <a href="{{ back_url }}" style="display:inline-block; padding:10px 14px; border:1px solid #e5e7eb; border-radius:10px; text-decoration:none; font-weight:700;">
    ← Volver al catálogo
  </a>
</div>

{% section 'soltex-product' %}
