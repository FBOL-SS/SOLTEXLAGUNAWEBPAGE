
{% schema %}
{
  "name": "Static content",
  "settings": [
    { "type": "text", "id": "title", "label": "Title", "default": "Quiénes somos" },
    { "type": "richtext", "id": "content", "label": "Content", "default": "<p>Agrega tu historia y valores.</p>" }
  ]
}
{% endschema %}
<section style="padding:40px 0">
  <div class="container">
    <h1 class="section-title">{{ section.settings.title }}</h1>
    <div class="section-sub" style="max-width:800px">{{ section.settings.content }}</div>
  </div>
</section>
